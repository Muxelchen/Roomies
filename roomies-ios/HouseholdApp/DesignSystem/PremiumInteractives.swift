import SwiftUI

// This file previously duplicated global interactive styles. They were consolidated into `PremiumDesignSystem.swift`.
// Keeping the file as a shim to avoid project file reference issues, but all implementations live in the design system.


