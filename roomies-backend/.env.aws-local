# Local AWS config (no secrets)
AWS_ENABLED=true
AWS_REGION=eu-central-1
# Rely on default credentials from AWS CLI/Sso; leave these empty for local use
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional bucket name if you already have one; otherwise backend features using S3 will be limited
# AWS_S3_BUCKET=roomies-storage

